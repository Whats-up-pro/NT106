<Window x:Class="ThreeMess.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ThreeMess.ViewModels"
        mc:Ignorable="d"
        Title="Quên mật khẩu" Height="420" Width="420" WindowStartupLocation="CenterScreen"
        Background="#FFF5F6F8" Opacity="0">

    <Window.DataContext>
        <vm:ForgotPasswordViewModel />
    </Window.DataContext>

    <Grid Margin="18">
        <Border Background="White" CornerRadius="18" Padding="26" BorderBrush="#FFE5E7EB" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Button Click="OpenLogin_Click" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Padding="0" Margin="0,0,0,8">
                    <TextBlock Text="←" FontSize="18" Foreground="#FF111827" />
                </Button>

                <TextBlock Text="Quên mật khẩu" FontSize="24" FontWeight="SemiBold" Foreground="#FF111827" HorizontalAlignment="Center" />
                <TextBlock Grid.Row="1" Text="Nhập email để nhận link khôi phục" Foreground="#FF6B7280" FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,18" />

                <TextBox Grid.Row="2" Style="{StaticResource WatermarkTextBox}" Tag="Email"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Row="3" Command="{Binding SendCommand}" Height="42" Margin="0,16,0,0"
                        Background="#FF38BDF8" Foreground="White" BorderThickness="0">
                    <TextBlock Text="Gửi" FontWeight="SemiBold" FontSize="14" />
                </Button>

                <TextBlock Grid.Row="4" Text="{Binding Status}" Foreground="#FFDC2626" Margin="0,12,0,0" TextWrapping="Wrap" />

                <Button Grid.Row="4" Click="OpenLogin_Click" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                        Background="Transparent" BorderThickness="0" Padding="0" Margin="0,0,0,6">
                    <TextBlock Text="Quay lại Login" Foreground="#FF2563EB" FontSize="12" />
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>


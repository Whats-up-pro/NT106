<Window x:Class="ThreeMess.ImagePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xem ảnh" Width="900" Height="650" MinWidth="520" MinHeight="420"
        WindowStartupLocation="CenterOwner">
    <Grid Background="#FFF5F6F8" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="White" CornerRadius="12" BorderBrush="#FFE5E7EB" BorderThickness="1" Padding="10">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="PreviewImage" Stretch="Uniform" />
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="140" Height="36" Click="SaveButton_Click"
                    Background="#FF2563EB" Foreground="White" BorderThickness="0" Cursor="Hand">
                <TextBlock Text="Lưu ảnh" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Button>
        </StackPanel>
    </Grid>
</Window>
